# GEO Stack Preview Check

The local preview confirms that the revised page loads the editorial image, hero record, six-layer specimen, reading rail, and Connect navigation CTA. The stronger visual system is materially more deliberate than the prior template-led rendering.

During desktop validation, the hero frame remained constrained by a legacy wrapper cascade despite the page-specific reset. The page wrapper has now been explicitly set to `width: 100vw`, `max-width: 100vw !important`, zero margin, and zero padding. The next reload will confirm that this final full-bleed correction is applied before publication.
